# Backend API URL
# Local dev: leave empty — Vite's dev server proxies /api → localhost:8000
# VITE_API_BASE_URL=

# Production (Vercel): set this to your Railway backend URL
# VITE_API_BASE_URL=https://your-backend.up.railway.app
